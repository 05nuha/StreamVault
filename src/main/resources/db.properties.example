# Copy this file to db.properties (same directory) and fill in real values.
# db.properties is gitignored so credentials never reach version control.
db.url=jdbc:mysql://localhost:3306/StreamVault?useSSL=false&serverTimezone=UTC
db.user=streamvault_app
db.password=change-me
